{
  "name": "@pint/finalization",
  "version": "1.0.0",
  "description": "Test the finalization in parachain intergration",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "@types/node": "^15.3.1",
    "ts-node": "^10.0.0",
    "tslint": "^6.1.3",
    "typescript": "^4.2.4"
  },
  "dependencies": {
    "@pint/e2e": "^1.0.0",
    "find-up": "^5.0.0"
  },
  "scripts": {
    "start": "ts-node index.ts",
    "build": "tsc --strict",
    "lint": "tsc --noEmit --strict && tslint --project ./tsconfig.json"
  }
}
